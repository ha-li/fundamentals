package com.gecko;

/**
 * Created by hlieu on 12/10/16.
 */
public class StringReverse {

   public String reverse (String s) {
      int length = s.length ();

      // this is impossible to do in java because strings are final class
      // so you cannot replace a char in the string, you would have
      // to create a new string, so its not very convenient
      for (int i = 0; i < length/2; i++ ){

      }

      return "impossible in java";
   }
}
